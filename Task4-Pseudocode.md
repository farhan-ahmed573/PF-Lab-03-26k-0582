START

Declare name as string
Declare rollNumber as integer
Declare age as integer
Declare height as float
Declare gpa as float
Declare section as character

Input student name
Input roll number
Input age
Input height
Input GPA
Input section

Display student information
Display name
Display roll number
Display age
Display height with 2 decimal places
Display GPA with 2 decimal places
Display section

END
